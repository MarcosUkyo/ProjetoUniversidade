@model ProjetoUniversidade.Models.Departamento
@{ ViewData["Title"] = "Editar Departamento"; }
<div class="page-header"><h1>Editar Departamento</h1></div>
<div class="card" style="max-width:500px">
    <form asp-action="Editar" method="post">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="IdDepto" />
        <div class="form-grid">
            <div class="field">
                <label asp-for="Nome"></label>
                <input asp-for="Nome" class="input" />
                <span asp-validation-for="Nome" class="text-danger"></span>
            </div>
            <div class="field">
                <label asp-for="Sigla"></label>
                <input asp-for="Sigla" class="input" maxlength="10" />
                <span asp-validation-for="Sigla" class="text-danger"></span>
            </div>
        </div>
        <div class="form-actions">
            <button type="submit" class="btn btn-primary">Salvar</button>
            <a asp-action="Index" class="btn btn-light">Cancelar</a>
        </div>
    </form>
</div>
@section Scripts { @{await Html.RenderPartialAsync("_ValidationScriptsPartial");} }